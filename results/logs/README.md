# Execution Logs

Populated automatically when `../../group_pipeline.ipynb` is run: produces `preprocessing_log.json`, a
machine-readable summary of the pipeline's effect on the data (shapes, counts of removed outliers/features,
PCA component count).
